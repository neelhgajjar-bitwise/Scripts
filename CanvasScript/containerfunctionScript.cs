using UnityEngine;
using System.Collections;

public class containerfunctionScript : MonoBehaviour
{
    Vector3 finger_pos, curPosition;
    bool commentval = false;

    void Start ()
    {
      //  TweenAlpha.Begin(GameObject.Find("ColorContainer_"), 0, 0);
      //  TweenAlpha.Begin(GameObject.Find("RemarksContainer_"),0,0);
    }
}
